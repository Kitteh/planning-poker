export default class PlanningLogger 
{
    error = (msg) => {
        console.log(msg);
    }

    warn = (msg) => {
        console.log(msg);
    }

    info = (msg) => {
        console.log(msg);
    }

    debug = (msg) => {
        console.log(msg);
    }
}